{
  "name": "websocket-relay-server",
  "version": "1.0.0",
  "description": "A simple WebSocket relay server for Render.",
  "main": "server.js",
  "scripts": {
    "start": "node --max-http-header-size=131072 server.js"
    "build": "npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
